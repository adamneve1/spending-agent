services:
  spending-agent:
    build: .
    container_name: spending-agent
    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - ${GOOGLE_CREDENTIALS_FILE:-./credentials.json}:/app/credentials.json:ro
      - report-scheduler-state:/app/data

volumes:
  report-scheduler-state:
